{
  "name": "private-stellar-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "ZK mixer protocol for Stellar — circuit build tooling",
  "scripts": {
    "build:circuit": "circom circuits/withdraw.circom --r1cs --wasm --sym -o circuits/build/",
    "setup": "bash scripts/setup_ceremony.sh",
    "test:circuit": "node tests/circuits/test_withdraw.js",
    "export:vk": "node scripts/export_vk_rust.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.5"
  }
}
